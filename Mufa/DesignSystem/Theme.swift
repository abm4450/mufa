import SwiftUI

enum MufaTheme {
    static let backgroundTop = Color(red: 0.91, green: 0.93, blue: 0.95)
    static let backgroundMid = Color(red: 0.94, green: 0.96, blue: 0.97)
    static let primary = Color(red: 47 / 255, green: 58 / 255, blue: 143 / 255)
    static let primaryDark = Color(red: 30 / 255, green: 42 / 255, blue: 107 / 255)
    static let accent = Color(red: 76 / 255, green: 201 / 255, blue: 240 / 255)
    static let foreground = Color(red: 31 / 255, green: 41 / 255, blue: 55 / 255)
    static let muted = Color(red: 107 / 255, green: 114 / 255, blue: 128 / 255)
    static let destructive = Color(red: 239 / 255, green: 68 / 255, blue: 68 / 255)
    static let cardBackground = Color.white.opacity(0.92)
    static let cornerRadius: CGFloat = 16
}
